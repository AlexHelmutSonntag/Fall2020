// An example of Interface 
// By default all of the fields are public, static and final. By default all of the methods public and abstract(except static and default)
interface Printable {
    int glas =12;
    void print();
    void draw();
}